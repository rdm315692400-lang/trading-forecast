# V7 Full
פרויקט מלא ל-Render + FastAPI + PWA.

Render:
Build Command: pip install -r requirements.txt
Start Command: uvicorn app.main:app --host 0.0.0.0 --port $PORT

Environment:
MASSIVE_API_KEY = המפתח החדש שלך (להכניס ב-Render Environment בלבד).

אין להעלות Service Account private key ל-GitHub.
